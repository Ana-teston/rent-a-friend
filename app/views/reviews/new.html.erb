<div class="container mt-5">
  <%= simple_form_for [@friend, @booking, @review] do |f| %>
  <%= f.input :title %>
  <%= f.input :body %>
  <%= f.input :rating %>
  <%= f.button :submit, 'Create Review', class: 'btn btn-primary mt-3' %>
<% end %>
</div>
